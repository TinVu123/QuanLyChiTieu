<Window x:Class="QuanLyChiTieu.View.EditTransactionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyChiTieu.View"
        mc:Ignorable="d"
        Title="EditTransactionView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Mô Tả" />
            <TextBox Text="{Binding SelectedTransaction.Description}" />

            <TextBlock Text="Số Tiền" Margin="0,10,0,0" />
            <TextBox Text="{Binding SelectedTransaction.Amount}" />

            <Button Content="Lưu" Command="{Binding SaveEditCommand}" Margin="0,20,0,0" HorizontalAlignment="Right" Width="75"/>
        </StackPanel>
    </Grid>
</Window>
